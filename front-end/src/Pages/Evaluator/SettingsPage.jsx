import React from 'react';
import SettingsPageComponent from '../Organization/SettingsPage';

// ✅ Reuse the same settings page for evaluators
const SettingsPage = () => {
  return <SettingsPageComponent />;
};

export default SettingsPage;